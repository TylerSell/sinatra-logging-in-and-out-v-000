<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= Helpers.current_user(session).username %></h1>
      <h3>Your account balance is: <%= Helpers.current_user(session).balance %></h3>
      <a href="/logout">Log Out</a>
    <% else %>
      <h2>You must <a href="/">Login</a> to view your balance.</h2>
    <% end %>
  </body>
</html>